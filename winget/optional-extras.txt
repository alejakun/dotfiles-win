# ================================================================================
# Optional group - Remote support tools and Earth Pro
# ================================================================================
# Read by install.ps1. The three fields below drive the prompt:
#
# Label:   Remote support tools and Earth Pro
# Offer:   mini
# Default: mini, base
#
# "Offer" is the lowest rung this group is offered from. "Default" lists the
# profiles where the answer defaults to yes; anywhere else it defaults to no.
# Defaulting to no is deliberate: undoing an unwanted install costs more than
# running again when you notice something missing.

# Remote Access
TeamViewer.TeamViewer
AnyDesk.AnyDesk

# Geographic Tools
#
# [STANDBY] Google.EarthPro - apagado a propósito, no retirado.
# La versión reciente de AutoCAD/CivilCAD, que se instala a mano en las máquinas
# donde hace falta, ya trae su propio Google Earth. Instalarlo aparte duplicaría
# algo que llega con el CAD.
#
# El ID es válido: descomentar lo reactiva. Se comenta en vez de borrarse
# justamente por eso - a diferencia de VMware o Krisp, que no existen en winget
# y por eso viven solo en MANUAL_INSTALL.md.
# Google.EarthPro
