# ================================================================================
# Windows Package Installation List - Max Profile
# ================================================================================
# Install via: winget install --id <PackageId>
#
# For machines that run infrastructure locally. Resource-intensive.
# Installing this profile also installs mini, base, plus and pro.
#
# Note: Ansible is not available via winget and does not support Windows as a
# control node - see MANUAL_INSTALL.md for the WSL route.

# Containers & Virtualization
Docker.DockerDesktop
Hashicorp.Vagrant
